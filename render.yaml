services:
  - type: web
    name: lambek-webapp
    runtime: python
    pythonVersion: "3.11"
    buildCommand: pip install -r webapp/requirements.txt
    startCommand: uvicorn webapp.backend:app --host 0.0.0.0 --port $PORT
